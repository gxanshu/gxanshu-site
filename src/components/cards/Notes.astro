---
const { data, url, index } = Astro.props;
---

<div
  data-aos="fade-up"
  data-aos-delay={`${index}00`}
  class="p-4 rounded-md border-2 hover:border-2 hover:border-black transition delay-75 duration-75 ease-in-out hover:shadow-xl focus:outline-none focus:ring bg-[#F6F6F6]"
>
  <h4 class="font-heading font-semibold py-4">
    <a href={`/notes/${url}`}>{data.title}</a>
  </h4>
  <p class="font-text text-sm/relaxed text-gray-500">{data.description}</p>
</div>
